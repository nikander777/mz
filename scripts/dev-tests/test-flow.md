# Тестовый план исправления аутентификации

## Тестовые данные
- Email: nikander@me.com
- Password: password
- Телефон подтвержден: ✅

## Тест 1: Логин через веб
1. Перейти на http://localhost:3000/login
2. Ввести данные и войти
3. Проверить что пользователь авторизован

## Тест 2: Переход на /messages
1. После логина перейти на http://localhost:3000/messages
2. Проверить что страница загружается без ошибок

## Тест 3: Обновление страницы /messages
1. На странице /messages нажать F5
2. Проверить что пользователь ОСТАЕТСЯ авторизованным
3. Проверить что не происходит редирект на /login

## Тест 4: WebSocket соединения
1. На странице /messages открыть DevTools
2. Проверить что Echo подключается к Reverb
3. Проверить что нет ошибок авторизации каналов

## Изменения сделанные:

### 1. SESSION_DOMAIN исправлен
- Было: `SESSION_DOMAIN=localhost`
- Стало: `SESSION_DOMAIN=` (пустое)

### 2. Переход на nuxt-auth-sanctum
- Заменил custom useAuth() на useSanctumAuth()
- Упростил middleware auth.ts и guest.ts
- Обновил все компоненты (AppHeader, app.vue, middleware)

### 3. API endpoint /api/auth/me исправлен
- Было: `{"success": true, "user": {...}}`
- Стало: `{id: 1, name: "...", ...}` (напрямую объект пользователя)

### 4. SSR проблемы решены
- Использование ClientOnly в messages/index.vue
- Устранены конфликты гидратации

## Ожидаемый результат:
✅ Аутентификация сохраняется при обновлении страницы
✅ WebSocket соединения работают стабильно
✅ Real-time сообщения функционируют